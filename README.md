# filler-words
Menu bar application that identifies and alerts when filler words (um, uh, etc) are used in speech. Start/stop a session manually, transcribe speech-to-text while running, detect a fixed list of filler words, show a simple running count + end-of-session summary.
